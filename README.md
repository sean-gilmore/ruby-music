# Ruby Music test project

## Getting started

Run the following two commands to install dependencies, and create the song input in "song.rb"

1. Run: docker build . -t ruby-music
2. Run: docker run -v $(pwd):/ruby ruby-music

